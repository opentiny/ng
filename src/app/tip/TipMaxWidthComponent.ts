import { Component } from '@angular/core';

@Component({
  templateUrl: './tip-max-width.html',
})
export class TipMaxWidthComponent {
  tipStr: string = 'this is my tip, it is a long tip, its maxWidth is defined';
}
